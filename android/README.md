# Legacy Android-only runtime

The supported client is the **Kotlin Multiplatform** module:

[`../kmp`](../kmp)

Use `JetForgeScreen` / `JetForgeComponent` from `:jetforge` (Android + iOS, shared Compose renderer). This folder is an earlier Android snapshot and is not the integration path for new apps.
